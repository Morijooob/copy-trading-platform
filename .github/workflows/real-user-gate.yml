name: Real User Gate

on:
  push:
    branches: [main]
    paths:
      - "src/real-user-account.js"
      - "test/real-user-account.test.js"
      - ".github/workflows/real-user-gate.yml"
  workflow_dispatch:

jobs:
  real-user-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node test/real-user-account.test.js
