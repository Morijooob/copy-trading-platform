const masters=[{id:1,name:'Atlas Demo',return:'+12.4%',risk:'کم',followers:2,queue:0,desc:'استراتژی آزمایشی با ریسک محدود و مناسب تست اولیه.'},{id:2,name:'Momentum Demo',return:'+8.7%',risk:'متوسط',followers:1,queue:0,desc:'مستر آزمایشی برای بررسی رفتار در بازار پرنوسان.'},{id:3,name:'Steady Demo',return:'+5.2%',risk:'کم',followers:0,queue:0,desc:'تمرکز بر ثبات و تعداد معاملات کمتر.'}];
const mastersEl=document.querySelector('#masters');
function render(){mastersEl.innerHTML='';masters.forEach(m=>{const el=document.createElement('article');el.className='master card';const full=m.followers>=2;el.innerHTML=`<span class="eyebrow">DEMO MASTER</span><h3>${m.name}</h3><p>${m.desc}</p><div class="metrics"><span>بازده ${m.return}</span><span>ریسک ${m.risk}</span></div><div class="capacity">فعال: ${m.followers}/2 · صف: ${m.queue}</div><button>${full?'ورود به صف انتظار':'شروع کپی دمو'}</button>`;el.querySelector('button').onclick=()=>follow(m.id);mastersEl.appendChild(el)});document.querySelector('#queueBadge').textContent=`صف کل: ${masters.reduce((n,m)=>n+m.queue,0)}`}
function follow(id){const m=masters.find(x=>x.id===id);if(m.followers<2){m.followers++;document.querySelector('#exchange').textContent='Demo Exchange';document.querySelector('#mode').textContent='دمو فعال';alert(`کپی دمو برای ${m.name} فعال شد.`)}else{m.queue++;alert(`ظرفیت ${m.name} پر است؛ شما در صف انتظار شماره ${m.queue} قرار گرفتید.`)}render()}
document.querySelector('#demoBtn').onclick=()=>{document.querySelector('#exchange').textContent='Demo Exchange';document.querySelector('#mode').textContent='دمو فعال';alert('محیط دمو فعال شد؛ هیچ سفارش واقعی ارسال نمی‌شود.')};render();
