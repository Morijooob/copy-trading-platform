const MAX_FOLLOWERS=2;
const masters=[
  {id:1,name:'Atlas Demo',return:'+12.4%',risk:'کم',followers:0,queue:0,desc:'استراتژی آزمایشی با ریسک محدود و مناسب تست اولیه.'},
  {id:2,name:'Momentum Demo',return:'+8.7%',risk:'متوسط',followers:0,queue:0,desc:'مستر آزمایشی برای بررسی رفتار در بازار پرنوسان.'},
  {id:3,name:'Steady Demo',return:'+5.2%',risk:'کم',followers:0,queue:0,desc:'تمرکز بر ثبات و تعداد معاملات کمتر.'}
];
const joined=new Set(),queued=new Set();
const DEMO_KEY='ct_demo_account_v6',SESSION_KEY='ct_demo_session_v2';
let demoAccount=null,authMode='login',toastTimer=null;
const $=s=>document.querySelector(s);
const mastersEl=$('#masters'),authModal=$('#authModal'),authError=$('#authError'),toast=$('#toast');
function fa(n){return String(n).replace(/\d/g,d=>'۰۱۲۳۴۵۶۷۸۹'[d]);}
function showToast(message){clearTimeout(toastTimer);toast.textContent=message;toast.classList.add('show');toastTimer=setTimeout(()=>toast.classList.remove('show'),3200);}
function setAuthError(message=''){authError.textContent=message;authError.classList.toggle('show',!!message);}
async function hashPassword(password){const data=new TextEncoder().encode(password);const digest=await crypto.subtle.digest('SHA-256',data);return Array.from(new Uint8Array(digest)).map(b=>b.toString(16).padStart(2,'0')).join('');}
function loadDemoAccount(){try{const raw=localStorage.getItem(DEMO_KEY);if(!raw)return null;const a=JSON.parse(raw);return a&&typeof a.username==='string'&&typeof a.email==='string'&&typeof a.passwordHash==='string'?a:null;}catch(e){return null;}}
function saveDemoAccount(a){try{localStorage.setItem(DEMO_KEY,JSON.stringify(a));return true;}catch(e){return false;}}
function getSessionUsername(){try{return sessionStorage.getItem(SESSION_KEY)||'';}catch(e){return '';}}
function setSession(u){try{sessionStorage.setItem(SESSION_KEY,u);return true;}catch(e){return false;}}
function clearSession(){try{sessionStorage.removeItem(SESSION_KEY);}catch(e){}}
function restoreSession(){const a=loadDemoAccount();return a&&getSessionUsername()===a.username?a:null;}
function renderAccount(){const card=$('#userCard'),profile=$('#profileMenu'),auth=$('#authBtn');if(demoAccount){card.classList.remove('hidden');profile.classList.remove('hidden');auth.classList.add('hidden');$('#userName').textContent=demoAccount.username;$('#topUserName').textContent=demoAccount.username;$('#userState').textContent='حساب دمو فعال است · بدون پول واقعی';}else{card.classList.add('hidden');profile.classList.add('hidden');auth.classList.remove('hidden');auth.textContent='ثبت‌نام / ورود دمو';}}
function setAuthMode(mode){authMode=mode;const login=mode==='login';$('#loginTab').classList.toggle('active',login);$('#registerTab').classList.toggle('active',!login);$('#authTitle').textContent=login?'ورود به حساب دمو':'ثبت‌نام حساب دمو';$('#authDescription').textContent=login?'اگر قبلاً حساب دمو ساخته‌اید، نام کاربری و رمزتان را وارد کنید.':'نام کاربری، ایمیل و رمز عبور خود را برای ساخت حساب دمو وارد کنید.';$('#email').classList.toggle('hidden',login);$('#passwordConfirm').classList.toggle('hidden',login);$('#password').setAttribute('autocomplete',login?'current-password':'new-password');$('#authSubmit').textContent=login?'ورود به حساب دمو':'ساخت حساب دمو';setAuthError('');}
function openAuth(mode='login'){setAuthMode(mode);authModal.classList.remove('hidden');setTimeout(()=>$('#username').focus(),0);}
function closeAuth(){authModal.classList.add('hidden');setAuthError('');}
function setDemoActive(){$('#exchange').textContent='Demo Exchange · مجازی';$('#mode').textContent='دمو فعال';$('#mode').classList.add('demo');$('#real').textContent='قفل';}
function enterDemo(){setDemoActive();render();$('#masters').scrollIntoView({behavior:'smooth',block:'start'});if(!demoAccount){showToast('محیط دمو فعال شد؛ حساب کاربری هنوز ساخته نشده است. برای شروع ثبت‌نام کنید.');setTimeout(()=>openAuth('register'),350);return;}showToast(`محیط دمو برای ${demoAccount.username} فعال است؛ یک مستر را انتخاب کنید.`);}
function render(){mastersEl.innerHTML='';let totalActive=0,totalQueue=0;masters.forEach(m=>{const full=m.followers>=MAX_FOLLOWERS,j=joined.has(m.id),q=queued.has(m.id);totalActive+=m.followers;totalQueue+=m.queue;const el=document.createElement('article');el.className='master card';el.tabIndex=0;let label='شروع کپی دمو';if(full)label='ورود به صف انتظار';if(j)label='کپی دمو فعال است';if(q)label='در صف انتظار هستید';el.innerHTML=`<span class="eyebrow">DEMO MASTER</span><h3>${m.name}</h3><p>${m.desc}</p><div class="metrics"><span>بازده ${m.return}</span><span>ریسک ${m.risk}</span></div><div class="capacity"><span>فعال: ${fa(m.followers)} از ${fa(MAX_FOLLOWERS)}</span><span>در صف: ${fa(m.queue)}</span></div><button type="button" ${j||q?'disabled':''}>${label}</button>`;const b=el.querySelector('button');const activate=()=>{if(!j&&!q)follow(m.id);};el.addEventListener('click',activate);el.addEventListener('keydown',e=>{if((e.key==='Enter'||e.key===' ')&&e.target===el){e.preventDefault();activate();}});b.addEventListener('click',e=>{e.stopPropagation();activate();});mastersEl.appendChild(el);});$('#activeBadge').textContent=`فعال: ${fa(totalActive)}`;$('#queueBadge').textContent=`در صف: ${fa(totalQueue)}`;renderAccount();}
function follow(id){if(!demoAccount){openAuth('register');return;}const m=masters.find(x=>x.id===id);if(!m||joined.has(id)||queued.has(id))return;if(m.followers<MAX_FOLLOWERS){m.followers++;joined.add(id);setDemoActive();showToast(`کپی دمو برای ${m.name} فعال شد. هیچ سفارش واقعی ارسال نمی‌شود.`);}else{m.queue++;queued.add(id);setDemoActive();showToast(`ظرفیت ${m.name} پر است؛ شما در صف انتظار شماره ${fa(m.queue)} قرار گرفتید.`);}render();}
$('#demoBtn').onclick=enterDemo;$('#authBtn').onclick=()=>openAuth('login');$('#heroAuthBtn').onclick=()=>openAuth('register');$('#closeAuth').onclick=closeAuth;$('#loginTab').onclick=()=>setAuthMode('login');$('#registerTab').onclick=()=>setAuthMode('register');authModal.onclick=e=>{if(e.target===authModal)closeAuth();};document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!authModal.classList.contains('hidden'))closeAuth();});
$('#authSubmit').onclick=async()=>{const username=$('#username').value.trim(),email=$('#email').value.trim().toLowerCase(),password=$('#password').value,confirm=$('#passwordConfirm').value;setAuthError('');if(username.length<3){setAuthError('نام کاربری باید حداقل ۳ کاراکتر باشد.');return;}if(authMode==='register'&&!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)){setAuthError('یک ایمیل معتبر وارد کنید.');return;}if(password.length<4){setAuthError('رمز عبور باید حداقل ۴ کاراکتر باشد.');return;}if(authMode==='register'){if(password!==confirm){setAuthError('تکرار رمز عبور با رمز اصلی یکسان نیست.');return;}if(loadDemoAccount()){setAuthError('این مرورگر از قبل یک حساب دمو دارد. از بخش ورود استفاده کنید.');return;}const account={username,email,passwordHash:await hashPassword(password),emailVerified:false};if(!saveDemoAccount(account)||!setSession(username)){setAuthError('ذخیره حساب دمو در این مرورگر ممکن نیست.');return;}demoAccount=account;closeAuth();renderAccount();setDemoActive();showToast(`حساب دمو برای ${username} ساخته شد؛ حالا یک مستر را انتخاب کنید.`);$('#masters').scrollIntoView({behavior:'smooth',block:'start'});return;}const account=loadDemoAccount();if(!account){setAuthError('حساب دمو پیدا نشد. ابتدا ثبت‌نام کنید.');return;}if(account.username!==username||account.passwordHash!==await hashPassword(password)){setAuthError('نام کاربری یا رمز عبور اشتباه است.');return;}if(!setSession(username)){setAuthError('ورود دمو در این مرورگر ممکن نیست.');return;}demoAccount=account;closeAuth();renderAccount();setDemoActive();showToast(`خوش آمدید ${username}؛ محیط دمو فعال شد.`);$('#masters').scrollIntoView({behavior:'smooth',block:'start'});};
function logout(){demoAccount=null;clearSession();joined.clear();queued.clear();masters.forEach(m=>{m.followers=0;m.queue=0;});$('#mode').textContent='دمو خاموش';render();showToast('از حساب دمو خارج شدید.');}
$('#logoutBtn').onclick=logout;$('#topLogoutBtn').onclick=logout;demoAccount=restoreSession();if(demoAccount)setDemoActive();render();