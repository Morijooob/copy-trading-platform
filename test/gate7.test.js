import assert from 'node:assert/strict';
import { ExchangeOrderStateMachine, STATES } from '../src/exchange-order-state-machine.js';
const test = (name, fn) => { try { fn(); console.log(`PASS: ${name}`); } catch (err) { console.error(`FAIL: ${name}`); throw err; } };
test('happy path reaches FILLED', () => { const sm = new ExchangeOrderStateMachine(); sm.transition(STATES.SUBMITTING); sm.transition(STATES.OPEN); sm.transition(STATES.PARTIALLY_FILLED, { filledQty: 2 }); sm.transition(STATES.FILLED, { filledQty: 5 }); assert.equal(sm.state, STATES.FILLED); assert.equal(sm.isTerminal(), true); });
test('illegal transitions are rejected', () => { const sm = new ExchangeOrderStateMachine(); assert.throws(() => sm.transition(STATES.FILLED), /invalid transition/); sm.transition(STATES.SUBMITTING); sm.transition(STATES.REJECTED); assert.throws(() => sm.transition(STATES.OPEN), /terminal order/); });
test('UNKNOWN is fail-closed but reconcilable', () => { const sm = new ExchangeOrderStateMachine(STATES.SUBMITTING); sm.transition(STATES.UNKNOWN); sm.transition(STATES.OPEN); assert.equal(sm.state, STATES.OPEN); });
test('terminal states are immutable', () => { for (const terminal of [STATES.FILLED, STATES.CANCELED, STATES.REJECTED, STATES.EXPIRED]) { const sm = new ExchangeOrderStateMachine(STATES.SUBMITTING); sm.transition(terminal); assert.equal(sm.isTerminal(), true); assert.throws(() => sm.transition(STATES.OPEN), /terminal order/); } });
test('duplicate terminal fill cannot be applied twice', () => { const sm = new ExchangeOrderStateMachine(STATES.SUBMITTING); sm.transition(STATES.FILLED, { fillId: 'fill-1' }); assert.throws(() => sm.transition(STATES.FILLED, { fillId: 'fill-1' }), /terminal order/); });
test('restart preserves state and history', () => { const first = new ExchangeOrderStateMachine(); first.transition(STATES.SUBMITTING); first.transition(STATES.OPEN); first.transition(STATES.PARTIALLY_FILLED, { filledQty: 3 }); const snapshot = first.snapshot(); const restored = new ExchangeOrderStateMachine(); restored.restore(snapshot); assert.deepEqual(restored.snapshot(), snapshot); restored.transition(STATES.FILLED, { filledQty: 5 }); assert.equal(restored.state, STATES.FILLED); });
test('corrupt snapshots fail closed', () => { const sm = new ExchangeOrderStateMachine(); assert.throws(() => sm.restore({ state: STATES.FILLED, history: [{ state: STATES.NEW }] }), /mismatch|history/); assert.throws(() => sm.restore({ state: STATES.OPEN, history: [{ state: STATES.NEW }, { state: STATES.FILLED }] }), /history/); });
test('repeated partial fills remain PARTIALLY_FILLED', () => { const sm = new ExchangeOrderStateMachine(STATES.SUBMITTING); sm.transition(STATES.PARTIALLY_FILLED, { fillId: 'a', filledQty: 1 }); sm.transition(STATES.PARTIALLY_FILLED, { fillId: 'b', filledQty: 2 }); assert.equal(sm.state, STATES.PARTIALLY_FILLED); assert.equal(sm.history.length, 3); });
console.log('Gate 7: ALL TESTS PASSED');
